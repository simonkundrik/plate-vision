{
  "name": "plate-vision-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "fetch-model": "node scripts/fetch-model.mjs"
  },
  "dependencies": {
    "@plate-vision/client": "*",
    "onnxruntime-web": "~1.24.0"
  },
  "devDependencies": {
    "typescript": "~6.0.3",
    "vite": "^6.0.7",
    "vite-plugin-static-copy": "^2.2.0"
  }
}
