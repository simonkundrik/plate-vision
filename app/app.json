{
  "expo": {
    "name": "plate-vision",
    "slug": "plate-vision",
    "version": "0.1.0",
    "orientation": "portrait",
    "icon": "./assets/icon.png",
    "userInterfaceStyle": "dark",
    "newArchEnabled": true,
    "splash": {
      "image": "./assets/splash-icon.png",
      "resizeMode": "contain",
      "backgroundColor": "#0B0B0C"
    },
    "ios": {
      "supportsTablet": true,
      "bundleIdentifier": "dev.simonkundrik.platevision"
    },
    "android": {
      "adaptiveIcon": {
        "foregroundImage": "./assets/adaptive-icon.png",
        "backgroundColor": "#0B0B0C"
      },
      "package": "dev.simonkundrik.platevision",
      "edgeToEdgeEnabled": true
    },
    "plugins": [
      [
        "expo-camera",
        {
          "cameraPermission": "plate-vision uses the camera to photograph your meal. The photo is analysed on this device and is not uploaded."
        }
      ],
      "expo-dev-client",
      "onnxruntime-react-native"
    ],
    "extra": {
      "// model": "Where the app fetches the ONNX artifact and its manifest. Both are published on GitHub Releases rather than bundled: the model is tens of megabytes and would otherwise sit in every APK and in this repository. Left empty here so the repo carries no URL that pretends to be live; set them for a build via EXPO_PUBLIC_MODEL_URL / EXPO_PUBLIC_BUNDLE_URL or by editing this block.",
      "modelUrl": "",
      "bundleUrl": ""
    }
  }
}
